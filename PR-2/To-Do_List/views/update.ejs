<!DOCTYPE html>
<html>
<head>
  <title>Update Task</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
  <div class="card p-4 shadow-sm col-md-6 mx-auto">

    <h4>Update Task</h4>

    <form action="/update/<%= index %>" method="POST">

  <input type="text" name="task" class="form-control mb-2"
    value="<%= item.task %>" required>

  <select name="priority" class="form-select mb-2" required>
    <option <%= item.priority==='High'?'selected':'' %>>High</option>
    <option <%= item.priority==='Medium'?'selected':'' %>>Medium</option>
    <option <%= item.priority==='Low'?'selected':'' %>>Low</option>
  </select>

  <select name="status" class="form-select mb-3" required>
    <option <%= item.status==='Pending'?'selected':'' %>>Pending</option>
    <option <%= item.status==='Done'?'selected':'' %>>Done</option>
  </select>

  <button class="btn btn-success w-100">Update</button>
</form>


    <a href="/" class="btn btn-link mt-2">← Back to Home</a>
  </div>
</div>

</body>
</html>
